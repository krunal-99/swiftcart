import CarouselComponent from "../UI/CarouselComponent";
import { adData } from "../data/data";

const AdvertisementSection = () => {
  return <CarouselComponent carouselData={adData} />;
};

export default AdvertisementSection;
